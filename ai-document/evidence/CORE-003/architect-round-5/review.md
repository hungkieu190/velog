# CORE-003 independent review — round 5

Date: 2026-09-24. Reviewer: Codex Architect. Builder: Antigravity. Intake PASS: task revision 6 / round 5 / READY_FOR_REVIEW / blueprint PASS / Architect next actor; task, checklist, and README aligned; Builder report and listed logs present. Reviewer made no application or test implementation changes.

## Independent verification

- Reviewed the submitted diff for `CapabilitiesTest.php`, the fixture, both product-smoke scripts, and `architecture.md`, plus the relevant unchanged runtime code in `Capabilities.php` and `PostTypes.php`.
- `composer run lint`: exit 0 (PHPCS and PHPStan). `composer run test -- --display-skipped`: exit 0, 45 tests, 215 assertions, no skipped tests. Both shell scripts passed `bash -n`; `git diff --check` exited 0. Builder's retained Node workflow log reports 20/20 passing; this reviewer did not rerun that unrelated suite.
- Ran one isolated `product-smoke.sh --task=CORE-003 --wp-version=6.4.3`: exit 0. The owned temporary directory and its child processes were absent afterward. This run did not cover WordPress 6.7.2, PHP 8.1, the four negative controls, or parallel-run cleanup. The fixture printed PASS for native admin GET responses of HTTP 500 and REST collection responses of HTTP 301; these are not valid authorization proof. The reviewer run's full per-case output was not retained as a raw log.

## Findings

- **F-002 OPEN (P1)**: Exact option restoration code and mocked value/autoload test improved, but real failed activation/rollback remains unverified. The live fixture's injected exception is on the ledger update after a successful repair and only asserts `schema == 1`; it does not compare raw option existence/value/autoload, role grants, or in-memory role state before and after a mutation failure (`tests/fixtures/core-003-verify.php:288-329`). The new restore-failure test only asserts `install() === false`, not the resulting state (`tests/Unit/CapabilitiesTest.php:502-587`). Prove rollback with a state-changing, one-shot persistence failure and exact before/after snapshots; include failure cleanup and reactivation cases.
- **F-003 OPEN (P1)**: The HTTP matrix accepts 500 for native GET, and 301/302 for REST and some other denials (`tests/fixtures/core-003-verify.php:154-205`). The independent WordPress 6.4.3 run produced these codes while reporting PASS. A server error or redirect can conceal the intended authorization path. Require a known successful authenticated control for each tested role, assert the denial response's actual cause, reject 5xx, and validate redirects rather than counting all 3xx as authorization. Retain response and unchanged-state evidence. The live fixture still lacks exact lifecycle/collision/failure state comparisons.
- **F-004 OPEN (P1)**: The controls harness compares only exit code 1 and checks all `/tmp/velog-product-smoke.*` directories, so a preflight failure can satisfy the expected negative result and unrelated runs can fail the leak check (`tests/workflow/product-smoke-controls.sh:41-70`). On signal/cleanup it warns about still-live children without failing, never `wait`s to reap them, and removes logs on exit (`:14-39`, `:74-112`). The inner runner similarly warns but does not fail if PHP survives KILL (`product-smoke.sh:50-84`). Require per-mode cause markers from the relevant validator, owned process identity and verified stop/reap/absence, isolation from unrelated runs, and retained per-run logs.
- **F-005 PARTIAL (P2)**: The stated static/unit gates pass. New admin callback assertions are useful, but the restore failure test verifies only a false return; it does not prove rollback/cleanup invariants. Required failure and callback evidence remains incomplete until F-002/F-004 controls pass.
- **F-006 OPEN (P2)**: `architecture.md` corrections are accurate. The round-5 evidence directory contains lint, PHPUnit, workflow, shell syntax, and diff logs, but no raw per-case WordPress version/PHP runtime, HTTP response, negative-mode cause, lifecycle snapshot, or process cleanup logs. The report's claim of raw per-case evidence is unsupported. Retain these logs from the corrected runner and align report/task wording with what was actually run.

## Decision

CORE-003 CHANGES_REQUESTED. AC1–AC4 remain unaccepted; do not mark checkboxes complete. Preserve F-001 closed. The passing static/unit gates and one 6.4.3 smoke run establish useful progress, but AC2–AC4 require the correction evidence above before DONE.
