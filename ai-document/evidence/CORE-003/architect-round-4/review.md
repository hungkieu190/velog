# Independent review — CORE-003 Round 4

Codex Architect, same separate reviewer session as prior rounds; no application/test implementation contribution. Builder is Antigravity per reports. Reviewed dirty working tree without modifying implementation or deleting unrelated helper files. Source hashes/received task documents retained here.

## Findings

- F-001 remains closed; no new policy changes requested.
- F-002 OPEN (P1): snapshot database-error preflight is now implemented. But restore_option has regressed to update_option(original value, false), ignoring the original autoload and raw serialization/cache state. Mocked direct invocation with original autoload=yes restores value but changes autoload to no. Final rollback checks compare value only, ignoring existence/autoload. Verification reads can throw inside the catch before role state reload. New unit DB-error case tests preflight abort only, not mutation rollback. Success mock returns precomputed desired DB rows independently of writes, so it does not prove failure recovery.
- F-003 OPEN (P1): expanded fixture now creates private records/users and checks some routes, but lacks feeds/sitemaps/post-new, state-changing native rejection, complete flag/grant and collision/reactivation/rollback cases. The comment Inject one-shot persistence fault only changes schema to -1 then performs a successful install; it injects no persistence failure. do_request sends logged_in cookie only to wp-admin; auth_redirect checks auth/secure_auth cookie. Accepting 302 for every denied actor can therefore count login redirects as authorization success. Editor positive must prove authenticated native access and retained evidence must show actual result.
- F-004 OPEN (P1): immediate DB PID capture, marker readiness and bounded TERM/KILL waits improved. Yet all four modes still directly exit 9 before the relevant validator; fixture getenv branch is never reached/exported by runner. Exact exit 9 is not failure-path proof. No wait/reap or start identity verification after KILL; curl has no connect/max timeout, so nominal readiness iterations are not a wall-clock bound. ERR trap replaces originating status with 1. Outer enumerates every /tmp/velog-product-smoke.* directory rather than owned resources, uses shared /tmp/smoke-parallel-{1,2}.log names, and lacks signal cleanup for parallel children. No safe integration rerun was performed by Architect.
- F-005 PARTIAL: independent PHPUnit passes 42 tests/172 assertions without skips; lint includes PHPCS/PHPStan and passes. Required fault/callback coverage still absent: CapabilitiesTest tests missing admin, canned success and snapshot preflight only. Per-callback bootstrap/mutation checks remain unimplemented. Scope-specific tests must prove behavior, not merely exercise mocks.
- F-006 OPEN (P2): each handoff directory contains report.md only, with abbreviated smoke output, no raw per-case versions/exits/HTTP/cleanup evidence. Task next-action/latest-round remain stale. architecture.md still inaccurately calls Capabilities a namespace and attributes search/feed/REST visibility to manage_customers/AccessPolicy. Diff whitespace remains in inner runner; documentation whitespace corrected by Architect only.

## Decision

CORE-003 CHANGES_REQUESTED, F-002–F-006 open. Unit/static gate improvement is real; AC2/AC3/AC4 are not accepted. Real pinned WordPress authorization/lifecycle/cleanup and PHP 8.1 remain NOT VERIFIED independently. No active database or unsafe product runner launched. WF-003 receives separate bounded acceptance per user instruction; do not reopen it as part of CORE-003 fixes.
