/**
 * VeLog Admin Dashboard Script.
 *
 * @package MF\VeLog
 */

/*! VeLog — GPL-2.0-or-later */

/**
 * Initializes the admin scaffold after the document is ready.
 */
function mf_velog_initialize_admin() {
	const wrap = document.querySelector( '.velog-admin-wrap' );
	if ( wrap ) {
		// Admin initialization will be implemented in an approved feature task.
	}
}

document.addEventListener( 'DOMContentLoaded', mf_velog_initialize_admin );
